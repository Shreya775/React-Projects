import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";
//use hooks inside functional component

ReactDOM.render(
  <App/>,
  document.getElementById("root")
);
